:80 {
    # Reverse proxy for backend API endpoints
    handle /api/* {
        reverse_proxy backend:8000
    }

    # Reverse proxy for frontend application (supports Vite HMR WebSocket transparently)
    handle {
        reverse_proxy frontend:5173
    }
}



